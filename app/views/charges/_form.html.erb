<%= simple_form_for charge do |f| %>
  <%= f.input :amount %>
  <%= f.input :description %>
  <%= f.input :operation_date, as: :date, order: [:day, :month, :year] %>
  <%= f.button :submit %>
<% end %>